%h1= t(:editing_post)

= error_messages_for :post

- form_for(:post, :url => object_url, :html => { :method => :put }) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag t(:update)

= link_to t(:show), object_path
|
= link_to t(:back_to_list), locale_posts_path(current_locale)

